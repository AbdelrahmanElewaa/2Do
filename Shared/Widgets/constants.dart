
enum Columns {
  id,
  title,
  content,
  createdAt,
  updatedAt,
  date,
}

const kDatabaseName = "animals.db";
const kDatabaseVersion = 1;

const kTableName = 'pets_tablee';

const kAppTitle = 'Pets Demo';
